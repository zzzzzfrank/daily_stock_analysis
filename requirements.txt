# ===================================
# A股自选股智能分析系统 - 依赖列表
# ===================================

# ------------------------------
# 核心基础依赖
# ------------------------------
python-dotenv>=1.0.0,<2.0.0        # 环境变量配置管理（密钥/API配置专用）
tenacity>=8.2.0,<9.0.0             # 重试机制（指数退避，API调用防超时）
sqlalchemy>=2.0.0,<3.0.0           # ORM数据库操作
schedule>=1.2.0,<2.0.0             # 定时任务调度
exchange-calendars>=4.5.0,<5.0.0   # 交易日历（A股/港股/美股，适配Issue #373）

# ------------------------------
# 数据源依赖（多源策略，按优先级排序）
# ------------------------------
efinance>=0.5.5,<1.0.0             # Priority 0: 东方财富数据源（最高优先级）https://github.com/Micro-sheep/efinance
akshare>=1.12.0,<2.0.0             # Priority 1: 东方财富爬虫数据源
tushare>=1.4.0,<2.0.0              # Priority 2: 挖地兔 Pro API
pytdx>=1.72,<2.0.0                 # Priority 2: 通达信行情服务器
baostock>=0.8.0,<1.0.0             # Priority 3: 证券宝数据
yfinance>=0.2.0,<0.3.0             # Priority 4: Yahoo Finance (Fallback 境外行情)

# ------------------------------
# 消息通知集成依赖
# ------------------------------
lark-oapi>=1.0.0,<2.0.0            # 飞书API
dingtalk-stream>=0.24.3,<1.0.0     # 钉钉 Stream SDK
discord.py>=2.0.0,<3.0.0           # Discord 机器人开发库

# ------------------------------
# 数据处理与格式解析
# ------------------------------
pandas>=2.0.0,<3.0.0               # 核心数据分析
numpy>=1.24.0,<2.0.0               # 数值计算
pypinyin>=0.50.0,<1.0.0            # Name-to-code resolver (拼音匹配股票名称转代码)
openpyxl>=3.1.0,<4.0.0             # Excel (.xlsx) 导入解析
json-repair>=0.55.1,<1.0.0         # 异常JSON自动修复
PyYAML>=6.0,<7.0                   # YAML 解析器（LITELLM_CONFIG 配置支持）

# ------------------------------
# AI 分析核心依赖【编码报错核心修复区】
# ------------------------------
# 修复说明：锁定版本区间解决cl100k_base编码加载异常，同时规避插件注册问题（Issue #537）
litellm>=1.82.0,<2.0.0             # 统一LLM客户端（兼容Gemini/Anthropic/OpenAI/DeepSeek/AIHubMix）
tiktoken>=0.8.0,<0.12.0            # BPE分词器，锁定<0.12.0规避插件问题，0.8.0+完整支持cl100k_base编码
openai>=1.0.0,<2.0.0               # OpenAI SDK（litellm传递依赖，显式声明保障AIHubMix兼容接口稳定）

# ------------------------------
# 舆情新闻与搜索引擎依赖
# ------------------------------
tavily-python>=0.3.0,<1.0.0        # Tavily 搜索 API（每月 1000 次免费额度）
google-search-results>=2.4.0,<3.0.0  # SerpAPI（每月 100 次免费额度）

# ------------------------------
# 网络请求与内容提取
# ------------------------------
requests>=2.31.0,<3.0.0            # 基础HTTP 请求
fake-useragent>=1.4.0,<2.0.0       # 随机 User-Agent 防爬虫封禁
httpx[socks]>=0.27.0,<1.0.0        # 异步HTTP 客户端 + SOCKS 代理支持（OpenAI/境外API可选依赖）
markdown2>=2.4.0,<3.0.0            # Markdown 转 HTML
imgkit>=1.2.0,<2.0.0               # Markdown 转图片（需提前安装 wkhtmltopdf 环境）
newspaper3k>=0.2.8,<1.0.0          # 新闻文章正文提取
lxml_html_clean>=0.1.1,<1.0.0      # 修复高版本lxml的lxml.html.clean导入报错

# ------------------------------
# 报告渲染与Web服务
# ------------------------------
jinja2>=3.1.0,<4.0.0               # Jinja2 模板引擎（分析报告渲染）
fastapi>=0.109.0,<1.0.0            # 现代 Python Web 框架
uvicorn[standard]>=0.27.0,<1.0.0   # ASGI 服务器
python-multipart>=0.0.6,<1.0.0     # FastAPI 文件/表单上传支持

# ------------------------------
# 内置依赖说明
# ------------------------------
# SQLite 为 Python 内置模块，无需额外安装
